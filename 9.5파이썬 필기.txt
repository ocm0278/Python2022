9.5 파이썬

파이썬은 구글링이다

문자열 + 문자열 연산자 역활로만 가능

엑세스 그 행당하는 인덱스의 변수를 사용한다
라이브러리, 메소드, 펑션, 함수 = 같은 의미의 단어

인터프리터 = 즉시 번역 (exe가 남지 않는다
문자열 = string(객체,오브젝트,=>클래스라고 부른다)
메쏘드 = 함수

프로퍼티- 멤버변수 - 멤버베리어블 (varyable)
find() 함수를 이용해서 하위 문자열을 다른 문자열에서 탐색 가능
zero base 0~ 시작
하위 문자열을 차지 못하면 find()는 -1로 반환
TEXT 아스키(Ascii) 로 되어 있다
탭은 여러개의 스페이스를 한키로

s[1]


파이썬 리스트

get _pz

리스트 = 튜플(추가 불가능)-함수로 된 수 *차이

리스트는 인덱스(복수)

차원을 가장 많이 사용하는 것이 이미지

리스트 상수 (고정값)

리스트는 변경 가능
튜플은 변경 불가능
상수 리스트는 튜플 이다

strip
upper - 원본 불변(문자열 변환)
lower  

리스트의 길이는?

range 함수 사용하기
range 함수는 0부터 매개변수호 넣은 값보다 1 작은

문자열 연사= 리스트 연산

리스트를 처음 부터 만들기

-빈 리스트를 만들고 append메서드를 이용하여 원소를 추가할 수 있음

-리스트 안은 순서가 유지디고 새원소는 리스트 끝에 더해짐

-stuff=list() - 선언문
stuff.append('book')
stuff.append('99')
int(stuff)
파이썬에서 변수를 사용하려면 선언이 되어 있어야 한다

Google Class

리스트에는 순서가 있다

-리스트는 많은 아이템을 보관할 수 있고 사용자가 순서를 바꾸기 위해 별도의 행동을
하지 않는 한 아이템의 순서를 유지
-리스트는 정렬 가능
(예: 원소 간 순서 바꾸기)
-sort메서드는 "스스로를 정렬"하는 기능(문자열과는 다름) -리스트의 순서를 바꾸어 정렬해 준다
swap 공간 
공간을 확보하는 것이 초기화 과정
내장함수와 기본-Library - 인터프러터 키워드 - 키워드처럼 사용하는 함수다
인수 필요하고 최종적으로 리턴 값이 나온다

메소드 - class 속한 함수
내장함수와 메소드가 기능상 겹치는 경우가 많다
변수에 .함수가 연결
앞은 class.method
y라는 변수를 놓고 .을 눌렀을때 
파이썬의 모든 변수는 CLASS로 이루어져 있다

